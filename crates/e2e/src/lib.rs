//! Deliberately empty.
//!
//! The harness lives in `tests/`, not here: a lib target that used the
//! simulator would pull it back into `[dependencies]` and undo the placement
//! ADR-0005 relies on.
